| Iteration | What changed | Why | Eval result | Decision |
| --- | --- | --- | --- | --- |
| Concept-flow diagrams | Added `extractConceptFlow` LLM agent (responseSchema, grounded `sourceSentence` per node/edge); replaced the whitespace-split fake claim graph in `/api/topic` with it; rewrote `generateDiagram` as a deterministic layered flowchart with connected, labelled arrows; fixed `ExcalidrawCanvas` to render bound (centred) text and route skip edges around nodes. | Section diagrams were rendering sentence fragments in boxes instead of a real conceptual flow. | Not run | Keep — diagram now reflects concept relationships. |
| Real verification + editor | Implemented `reverseParseDiagram` + real `graphDiff` + real `groundingCheck`; `verifyDiagram` now blends structural diff (65%) and citation grounding (35%) into a real confidence instead of a hardcoded 0.9. Added `runSectionPipeline` with a feedback-driven single patch retry; wired it into `/api/topic` and section regenerate; orchestrator patch loop now actually re-extracts and keeps the better result. `ExcalidrawCanvas` gains a `mode` prop — `preview` (fit-to-content, chrome hidden) vs `editor` (full toolbar, theme toggle, export); `DiagramCard` has an "Edit diagram" toggle. Fixed dark canvas (removed the pre-inverted background override). Implemented `scoreJudge` as a real LLM equivalence check. | Confidence/diff were fabricated; users could not edit the diagram; canvas rendered light. | Manual smoke: grounded section → confidence 1.0; ungrounded citations → confidence 0.65 with per-item grounding errors. | Keep. |
| Claude provider | `lib/llm/provider.ts` now routes through Anthropic Claude when `ANTHROPIC_API_KEY` is set (forced `record_response` tool with `strict: true` + JSON-Schema `input_schema` converted from the existing Gemini schemas), falling back to Gemini otherwise. No call sites changed. Added `@anthropic-ai/sdk`; `.env.local` gained `ANTHROPIC_API_KEY` / `ANTHROPIC_MODEL` (default `claude-opus-5`). | Gemini free-tier quota was exhausted. | Not run (needs a key). | Keep — provider-agnostic, either key works. |
| OpenAI-compatible provider | Added a third `generateStructured` backend: any OpenAI-compatible `/chat/completions` endpoint via `OPENAI_API_KEY` / `OPENAI_BASE_URL` / `OPENAI_MODEL` (schema injected into the system prompt, `response_format: json_object`, fence-tolerant parse — no new dependency, uses `fetch`). Priority: OpenAI-compatible → Anthropic → Gemini. Enables free backends: Groq (no card) and local Ollama. | Both paid providers became billing-blocked (Gemini quota, Anthropic zero credit). | Not run (needs a key). | Keep. |
| Review + notes export | Removed the Regenerate button; added a server-persisted manual `verified` flag (`POST /sections/[id]/verify`). Trimmed the detail modal to five explained metrics (confidence, recall, precision, edge fidelity, grounded citations) — dropped stage grid, retry-loop/patch-attempts duplicate, escalated tile, and the redundant "Final verification" button; the card footer now shows only generation time + a clickable confidence figure. Section generation retries up to 2× before failing, and failed sections are excluded from export. New `/topic/[id]/notes` view renders verified sections as a clean printable document (heading + sentence bullets + diagram exported via `exportToSvg`), unlocked once every generated section is verified. | Verified state was duplicated in 3 places; the matrix had noise; PDF was a screen print. | Not run. | Keep. |
